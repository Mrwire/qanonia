apiVersion: v1
kind: Service
metadata:
  name: {{ include "qanounia.fullname" . }}-frontend
  labels: {{- include "qanounia.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "qanounia.name" . }}-frontend
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
